package bankapplication;

/*
 * Overview:
 * Dimensions is an interface (immutable), which provides constants for GUI dimensions used across the 
 * bank application's user interface.
 * This interface allows for consistent sizing of windows and components throughout the application.
 */

public interface Dimensions {
  int WIDTH = 1350;
  int HEIGHT = 800;
}